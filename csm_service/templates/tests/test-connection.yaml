apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "csm-service.fullname" . }}-test-connection"
  labels:
    {{- include "csm-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "csm-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
